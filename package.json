{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "mocha \"test/add.test.js\"",
    "cover:unit": "nyc --silent yarn test",
    "cover:report": "nyc report --reporter=lcov --reporter=text --report-dir='./jscoverage'",
    "cover:reporthtml": "nyc report --reporter=html --report-dir='./jscoverage'",
    "test:coverage": "yarn cover:unit && yarn cover:report",
    "test:coveragehtml": "yarn cover:unit && yarn cover:reporthtml"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  }
}
